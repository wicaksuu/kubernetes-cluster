apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wicaksu # ganti ini
  namespace: db
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: nfs-rwx
  resources:
    requests:
      storage: 100Gi
